<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation_graph"
    app:startDestination="@id/feed">

    <fragment
        android:id="@+id/feed"
        android:name="dev.pimentel.feed.presentation.FeedFragment"
        android:label="FeedFragment"
        tools:layout="@layout/feed_fragment_layout" />

    <fragment
        android:id="@+id/search"
        android:name="dev.pimentel.search.presentation.SearchFragment"
        android:label="SearchFragment"
        tools:layout="@layout/search_fragment_layout" />

    <fragment
        android:id="@+id/profile"
        android:name="dev.pimentel.profile.presentation.ProfileFragment"
        android:label="ProfileFragment"
        tools:layout="@layout/profile_fragment_layout" />

</navigation>
